# Продакшен-образ использует относительный путь `/api`,
# поэтому отдельная runtime-настройка для API не нужна.

# URL бэкенда для dev-прокси (используется в vite.config.ts)
VITE_BACKEND_URL=http://localhost:3001
